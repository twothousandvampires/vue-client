export default class FastMovement{
    constructor(template) {
        this.level = template.level
        this.name = template.name
        this.description = template.description
        this.type = template.type
        this.exp_needed = template.exp_needed
    }
}