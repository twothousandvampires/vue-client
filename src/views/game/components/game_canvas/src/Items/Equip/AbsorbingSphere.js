import Equip from "../Equip";

export default class AbsorbingSphere extends Equip{
    constructor(template) {
        super(template)
        this.img_path = '/src/assets/img/absorbing_sphere.gif'
    }
}