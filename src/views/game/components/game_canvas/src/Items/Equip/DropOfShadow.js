import Equip from "../Equip";

export default class DropOfShadow extends Equip{
    constructor(template) {
        super(template);
        this.img_path = '/src/assets/img/drop_of_shadow.gif'
    }
}